'use strict';

var folder = require('./folderControl'),
    time = require('./time'),
    config = require('../config');

module.exports = {
    dispersion: () => {
        var path = config.path.image,

    }
}